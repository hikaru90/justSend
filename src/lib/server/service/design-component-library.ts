/**
 * Starter HTML kit removed — design-system components are authored as
 * email-builder block trees (see ComponentDocument / ComponentSlot).
 * This module is intentionally empty and kept only so old imports fail loudly
 * at the call site rather than silently resolving to missing symbols.
 */

export {};
