packages:
  - '.'

allowBuilds:
  # Native / bundler postinstalls we need
  better-sqlite3: true
  esbuild: true
  # Transitive from Pi SDK (@earendil-works/pi-ai) — Owlery uses OpenRouter, not Google
  '@google/genai': false
  protobufjs: false
